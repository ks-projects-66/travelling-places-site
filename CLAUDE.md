# Travelling Places interface instructions

The canonical brand system is `brand-kit-v2/BRAND.md`. Read it with `brand-kit-v2/AGENT-BRIEF.md` and `brand-kit-v2/brand.tokens.json` before interface work.

Use the supplied tokens, logos, fonts, and components. Run `pnpm brand:check` and `pnpm build` before completion. Keep current factual copy unless the user explicitly approves a change.
